# Win11Debloat feature reference

> Generated by `Scripts/Build-FeatureDocs.ps1` from `Config/Features.json` and the
> `Regfiles/` corpus. Do not edit by hand - rerun the script after changing a feature.

This reference lists every setting Win11Debloat can apply, the command-line switch that
selects it, and the exact registry values it writes, so you can see precisely what a
setting does before you run it.

149 settings across 13 categories.

## Contents

- [Privacy & Suggested Content](#privacy--suggested-content) (31)
- [System](#system) (8)
- [Network & Security](#network--security) (7)
- [Start Menu & Search](#start-menu--search) (12)
- [AI](#ai) (7)
- [Windows Update](#windows-update) (4)
- [Taskbar](#taskbar) (19)
- [Appearance](#appearance) (8)
- [File Explorer](#file-explorer) (22)
- [Gaming](#gaming) (2)
- [Multi-tasking](#multi-tasking) (7)
- [Optional Windows Features](#optional-windows-features) (2)
- [Other](#other) (12)
- [Other settings](#other-settings) (8)

## Privacy & Suggested Content

### Disable telemetry, tracking & targeted ads

`-DisableTelemetry`

Registry changes (`Disable_Telemetry.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\AdvertisingInfo` | `Enabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Privacy` | `TailoredExperiencesWithDiagnosticDataEnabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Speech_OneCore\Settings\OnlineSpeechPrivacy` | `HasAccepted` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Input\TIPC` | `Enabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\InputPersonalization` | `RestrictImplicitInkCollection` | 1 |
| `HKEY_CURRENT_USER\Software\Microsoft\InputPersonalization` | `RestrictImplicitTextCollection` | 1 |
| `HKEY_CURRENT_USER\Software\Microsoft\InputPersonalization\TrainedDataStore` | `HarvestContacts` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Personalization\Settings` | `AcceptedPrivacyPolicy` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\DataCollection` | `AllowTelemetry` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `Start_TrackProgs` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\System` | `PublishUserActivities` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\System` | `EnableActivityFeed` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\System` | `UploadUserActivities` | 0 |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Siuf\Rules` | `NumberOfSIUFInPeriod` | 0 |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Siuf\Rules` | `PeriodInNanoSeconds` | the value is deleted |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `PersonalizationReportingEnabled` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `DiagnosticData` | 0 |

Reverting: unchecking this setting applies `Enable_Telemetry.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable tips, tricks & suggested content throughout Windows

`-DisableSuggestions`

Registry changes (`Disable_Windows_Suggestions.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\ContentDeliveryManager` | `SubscribedContent-310093Enabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\ContentDeliveryManager` | `SubscribedContent-338388Enabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\ContentDeliveryManager` | `SystemPaneSuggestionsEnabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `Start_IrisRecommendations` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\ContentDeliveryManager` | `SubscribedContent-338389Enabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\ContentDeliveryManager` | `SoftLandingEnabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\ContentDeliveryManager` | `SubscribedContent-338393Enabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\ContentDeliveryManager` | `SubscribedContent-353694Enabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\ContentDeliveryManager` | `SubscribedContent-353696Enabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\ContentDeliveryManager` | `SubscribedContent-353698Enabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\SystemSettings\AccountNotifications` | `EnableAccountNotifications` | 0 |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\UserProfileEngagement` | `ScoobeSystemSettingEnabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `ShowSyncProviderNotifications` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\ContentDeliveryManager` | `SilentInstalledAppsEnabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Notifications\Settings\Windows.SystemToast.Suggested` | `Enabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Mobility` | `OptedIn` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `Start_AccountNotifications` | 0 |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Notifications\Settings\Windows.SystemToast.BackupReminder` | `Enabled` | 0 |

Reverting: unchecking this setting applies `Enable_Windows_Suggestions.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable syncing of Windows settings to your Microsoft account

`-DisableSettingsSync`

Registry changes (`Disable_Settings_Sync.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\SettingSync` | `DisableSettingSync` | 2 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\SettingSync` | `DisableSettingSyncUserOverride` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\SettingSync` | `DisableWindowsSettingSync` | 2 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\SettingSync` | `DisableSyncOnPaidNetwork` | 1 |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\SettingSync\Groups\Personalization` | `Enabled` | 0 |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\SettingSync\Groups\BrowserSettings` | `Enabled` | 0 |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\SettingSync\Groups\Credentials` | `Enabled` | 0 |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\SettingSync\Groups\Accessibility` | `Enabled` | 0 |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\SettingSync\Groups\Windows` | `Enabled` | 0 |

Reverting: unchecking this setting applies `Enable_Settings_Sync.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Microsoft Office telemetry & customer data

`-DisableOfficeTelemetry`

Registry changes (`Disable_Office_Telemetry.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Policies\Microsoft\office\16.0\common` | `sendcustomerdata` | 0 |
| `HKEY_CURRENT_USER\Software\Policies\Microsoft\office\16.0\common` | `qmenable` | 0 |
| `HKEY_CURRENT_USER\Software\Policies\Microsoft\office\common\clienttelemetry` | `sendtelemetry` | 3 |

Reverting: unchecking this setting applies `Enable_Office_Telemetry.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Windows Error Reporting

`-DisableErrorReporting`

Registry changes (`Disable_Error_Reporting.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\Windows Error Reporting` | `Disabled` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\Windows Error Reporting` | `DontShowUI` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\Windows Error Reporting` | `LoggingDisabled` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\Windows Error Reporting` | `DontSendAdditionalData` | 1 |
| `HKEY_CURRENT_USER\SOFTWARE\Policies\Microsoft\Windows\Windows Error Reporting` | `Disabled` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\DeviceInstall\Settings` | `DisableSendGenericDriverNotFoundToWER` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\DeviceInstall\Settings` | `DisableSendRequestAdditionalSoftwareToWER` | 1 |

Reverting: unchecking this setting applies `Enable_Error_Reporting.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable the Customer Experience Improvement Program

`-DisableCEIP`

Registry changes (`Disable_CEIP.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\SQMClient\Windows` | `CEIPEnable` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\AppV\CEIP` | `CEIPEnable` | 0 |

Reverting: unchecking this setting applies `Enable_CEIP.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable clipboard sync across devices

`-DisableCloudClipboard`

Registry changes (`Disable_Cloud_Clipboard.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\System` | `AllowCrossDeviceClipboard` | 0 |

Reverting: unchecking this setting applies `Enable_Cloud_Clipboard.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable text message sync to the cloud

`-DisableMessageSync`

Registry changes (`Disable_Message_Sync.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\Messaging` | `AllowMessageSync` | 0 |

Reverting: unchecking this setting applies `Enable_Message_Sync.reg`.

New user profiles: covered when running in Sysprep mode.

### Stop websites accessing your language list

`-DisableLanguageListAccess`

Registry changes (`Disable_Language_List_Access.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Control Panel\International\User Profile` | `HttpAcceptLanguageOptOut` | 1 |

Reverting: unchecking this setting applies `Enable_Language_List_Access.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable camera access from the lock screen

`-DisableLockscreenCamera`

Registry changes (`Disable_Lockscreen_Camera.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\Personalization` | `NoLockScreenCamera` | 1 |

Reverting: unchecking this setting applies `Enable_Lockscreen_Camera.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable automatic speech model updates

`-DisableSpeechModelUpdates`

Registry changes (`Disable_Speech_Model_Updates.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Speech` | `AllowSpeechModelUpdate` | 0 |

Reverting: unchecking this setting applies `Enable_Speech_Model_Updates.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Microsoft experiments on this device

`-DisableExperimentation`

Registry changes (`Disable_Experimentation.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\PolicyManager\default\System\AllowExperimentation` | `value` | 0 |

Reverting: unchecking this setting applies `Enable_Experimentation.reg`.

New user profiles: covered when running in Sysprep mode.

### Prevent Windows from reinstalling removed & promoted apps

`-PreventAppReinstall`

Registry changes (`Prevent_App_Reinstall.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\CloudContent` | `DisableWindowsConsumerFeatures` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\CloudContent` | `DisableCloudOptimizedContent` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\CloudContent` | `DisableSoftLanding` | 1 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\ContentDeliveryManager` | `ContentDeliveryAllowed` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\ContentDeliveryManager` | `FeatureManagementEnabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\ContentDeliveryManager` | `OemPreInstalledAppsEnabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\ContentDeliveryManager` | `PreInstalledAppsEnabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\ContentDeliveryManager` | `PreInstalledAppsEverEnabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\ContentDeliveryManager` | `SubscribedContentEnabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\ContentDeliveryManager` | `RemediationRequired` | 0 |

Reverting: unchecking this setting applies `Allow_App_Reinstall.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Windows Notifications (From apps and other senders)

`-DisableNotifications`

Registry changes (`Disable_Notifications.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\PushNotifications` | `ToastEnabled` | 0 |

Reverting: unchecking this setting applies `Enable_Notifications.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Windows location services & app location access

`-DisableLocationServices`

Registry changes (`Disable_Location_Services.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\LocationAndSensors` | `DisableLocation` | 1 |

Reverting: unchecking this setting applies `Enable_Location_Services.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Find My Device location tracking

`-DisableFindMyDevice`

Registry changes (`Disable_Find_My_Device.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\FindMyDevice` | `AllowFindMyDevice` | 0 |

Reverting: unchecking this setting applies `Enable_Find_My_Device.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable tips & tricks on the lock screen

`-DisableLockscreenTips`

Registry changes (`Disable_Lockscreen_Tips.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\ContentDeliveryManager` | `SubscribedContent-338387Enabled` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\ContentDeliveryManager` | `RotatingLockScreenOverlayEnabled` | 0 |

Reverting: unchecking this setting applies `Enable_Lockscreen_Tips.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Windows Spotlight for desktop

`-DisableDesktopSpotlight`

Registry changes (`Disable_Desktop_Spotlight.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Policies\Microsoft\Windows\CloudContent` | `DisableSpotlightCollectionOnDesktop` | 1 |

Reverting: unchecking this setting applies `Enable_Desktop_Spotlight.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable ads, suggestions and newsfeed in Edge

`-DisableEdgeAds`

Registry changes (`Disable_Edge_Ads_And_Suggestions.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `NewTabPageContentEnabled` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `NewTabPageHideDefaultTopSites` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `EdgeShoppingAssistantEnabled` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `TabServicesEnabled` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `AlternateErrorPagesEnabled` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `UserFeedbackAllowed` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `ShowRecommendationsEnabled` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `WalletDonationEnabled` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `HideFirstRunExperience` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `DefaultBrowserSettingEnabled` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `DefaultBrowserSettingsCampaignEnabled` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `SpotlightExperiencesAndRecommendationsEnabled` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `ShowAcrobatSubscriptionButton` | 0 |

Reverting: unchecking this setting applies `Enable_Edge_Ads_And_Suggestions.reg`.

New user profiles: covered when running in Sysprep mode.

### Hide Microsoft 365 Copilot ads in Settings Home

`-DisableSettings365Ads`

*Version-gated setting.*

Registry changes (`Disable_Settings_365_Ads.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\CloudContent` | `DisableConsumerAccountStateContent` | 1 |

Reverting: unchecking this setting applies `Enable_Settings_365_Ads.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable NVIDIA telemetry

`-DisableNvidiaTelemetry`

Registry changes (`Disable_Nvidia_Telemetry.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\NVIDIA Corporation\NVControlPanel2\Client` | `OptInOrOutPreference` | 0 |

Reverting: unchecking this setting applies `Enable_Nvidia_Telemetry.reg`.

New user profiles: covered when running in Sysprep mode.

### Deny app access to diagnostics, location and account info

`-DenyAppPermissions`

Registry changes (`Deny_App_Permissions.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\CapabilityAccessManager\ConsentStore\appDiagnostics` | `Value` | "Deny" |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\CapabilityAccessManager\ConsentStore\location` | `Value` | "Deny" |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\CapabilityAccessManager\ConsentStore\userAccountInformation` | `Value` | "Deny" |

Reverting: unchecking this setting applies `Allow_App_Permissions.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable device monitoring

`-DisableDeviceMonitoring`

Registry changes (`Disable_Device_Monitoring.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\DeviceHealthAttestationService` | `EnableDeviceHealthAttestationService` | 0 |

Reverting: unchecking this setting applies `Enable_Device_Monitoring.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Program Compatibility Assistant telemetry

`-DisableProgramCompatibilityAssistant`

Registry changes (`Disable_PCA.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\AppCompat` | `AITEnable` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\AppCompat` | `AllowTelemetry` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\AppCompat` | `DisableEngine` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\AppCompat` | `DisableInventory` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\AppCompat` | `DisablePCA` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\AppCompat` | `DisableUAR` | 1 |

Reverting: unchecking this setting applies `Enable_PCA.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Customer Experience Improvement tracking

`-DisableCustomerExperienceImprovement`

Registry changes (`Disable_CEIP_Tracking.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\WDI\{9c5a40da-b965-4fc3-8781-88dd50a6299d}` | `ScenarioExecutionEnabled` | 0 |

Reverting: unchecking this setting applies `Enable_CEIP_Tracking.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable the Windows privacy experience

`-DisablePrivacyExperience`

Registry changes (`Disable_Privacy_Experience.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\OOBE` | `DisablePrivacyExperience` | 1 |

Reverting: unchecking this setting applies `Enable_Privacy_Experience.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Resultant Set of Policy logging

`-DisableRsopLogging`

Registry changes (`Disable_RSOP_Logging.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\System` | `RSoPLogging` | 0 |

Reverting: unchecking this setting applies `Enable_RSOP_Logging.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable user activity tracking

`-DisableUserTracking`

Registry changes (`Disable_User_Tracking.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Explorer` | `NoInstrumentation` | 1 |

Reverting: unchecking this setting applies `Enable_User_Tracking.reg`.

New user profiles: covered when running in Sysprep mode.

### Disallow Microsoft account sign-in

`-DisallowMicrosoftAccounts`

Registry changes (`Disallow_Microsoft_Accounts.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System` | `NoConnectedUser` | 1 |

Reverting: unchecking this setting applies `Allow_Microsoft_Accounts.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Windows activation telemetry

`-DisableActivationTelemetry`

Registry changes (`Disable_Activation_Telemetry.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows NT\CurrentVersion\Software Protection Platform` | `NoGenTicket` | 1 |

Reverting: unchecking this setting applies `Enable_Activation_Telemetry.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable diagnostic tracing (DiagTrack)

`-DisableDiagnosticTracing`

Registry changes (`Disable_Diagnostic_Tracing.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\Diagnostics\Performance` | `DisableDiagnosticTracing` | 1 |

Reverting: unchecking this setting applies `Enable_Diagnostic_Tracing.reg`.

New user profiles: covered when running in Sysprep mode.

## System

### Disable 'Drag Tray' for sharing & moving files

`-DisableDragTray`

*Version-gated setting.*

Registry changes (`Disable_Share_Drag_Tray.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\CDP` | `DragTrayEnabled` | 0 |

Reverting: unchecking this setting applies `Enable_Share_Drag_Tray.reg`.

New user profiles: covered when running in Sysprep mode.

### Use classic Windows 10 context menu style

`-RevertContextMenu`

*Version-gated setting.*

Registry changes (`Disable_Show_More_Options_Context_Menu.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Classes\CLSID\{86ca1aa0-34aa-4e8b-a509-50c905bae2a2}\InprocServer32` | `(Default)` | "" |

Reverting: unchecking this setting applies `Enable_W11_Style_Context_Menu.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Enhance Pointer Precision (mouse acceleration)

`-DisableMouseAcceleration`

Registry changes (`Disable_Enhance_Pointer_Precision.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Control Panel\Mouse` | `MouseSpeed` | "0" |
| `HKEY_CURRENT_USER\Control Panel\Mouse` | `MouseThreshold1` | "0" |
| `HKEY_CURRENT_USER\Control Panel\Mouse` | `MouseThreshold2` | "0" |

Reverting: unchecking this setting applies `Enable_Enhance_Pointer_Precision.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Sticky Keys keyboard shortcut (5x shift)

`-DisableStickyKeys`

*Version-gated setting.*

Registry changes (`Disable_Sticky_Keys_Shortcut.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Control Panel\Accessibility\StickyKeys` | `Flags` | "506" |

Reverting: unchecking this setting applies `Enable_Sticky_Keys_Shortcut.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Storage Sense automatic disk cleanup

`-DisableStorageSense`

*Version-gated setting.*

Registry changes (`Disable_Storage_Sense.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\StorageSense\Parameters\StoragePolicy` | `01` | 0 |

Reverting: unchecking this setting applies `Enable_Storage_Sense.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable fast start-up

`-DisableFastStartup`

Registry changes (`Disable_Fast_Startup.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\Session Manager\Power` | `HiberbootEnabled` | 0 |

Reverting: unchecking this setting applies `Enable_Fast_Startup.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable BitLocker automatic device encryption

`-DisableBitlockerAutoEncryption`

*Version-gated setting.*

Registry changes (`Disable_Bitlocker_Auto_Encryption.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\BitLocker` | `PreventDeviceEncryption` | 1 |

Reverting: unchecking this setting applies `Enable_Bitlocker_Auto_Encryption.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Modern Standby network connectivity

`-DisableModernStandbyNetworking`

*Version-gated setting.*

Registry changes (`Disable_Modern_Standby_Networking.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Power\PowerSettings\f15576e8-98b7-4186-b944-eafa664402d9` | `ACSettingIndex` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Power\PowerSettings\f15576e8-98b7-4186-b944-eafa664402d9` | `DCSettingIndex` | 0 |

Reverting: unchecking this setting applies `Enable_Modern_Standby_Networking.reg`.

New user profiles: covered when running in Sysprep mode.

## Network & Security

### Disable LLMNR name resolution

`-DisableLLMNR`

Registry changes (`Disable_LLMNR.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows NT\DNSClient` | `EnableMulticast` | 0 |

Reverting: unchecking this setting applies `Enable_LLMNR.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Remote Assistance

`-DisableRemoteAssistance`

Registry changes (`Disable_Remote_Assistance.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\Remote Assistance` | `fAllowToGetHelp` | 0 |
| `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\Remote Assistance` | `fAllowFullControl` | 0 |

Reverting: unchecking this setting applies `Enable_Remote_Assistance.reg`.

New user profiles: covered when running in Sysprep mode.

### Block anonymous enumeration of accounts & shares

`-RestrictAnonymousAccess`

Registry changes (`Restrict_Anonymous_Access.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\Lsa` | `RestrictAnonymous` | 1 |
| `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\Lsa` | `RestrictAnonymousSAM` | 1 |
| `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\LanManServer\Parameters` | `RestrictNullSessAccess` | 1 |

Reverting: unchecking this setting applies `Allow_Anonymous_Access.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable automatic connection to suggested open hotspots

`-DisableWifiSense`

Registry changes (`Disable_Wifi_Sense.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\WcmSvc\wifinetworkmanager\config` | `AutoConnectAllowedOEM` | 0 |

Reverting: unchecking this setting applies `Enable_Wifi_Sense.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Windows Connect Now (WPS)

`-DisableWindowsConnectNow`

Registry changes (`Disable_Windows_Connect_Now.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\WCN\UI` | `DisableWcnUi` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\WCN\Registrars` | `EnableRegistrars` | 0 |

Reverting: unchecking this setting applies `Enable_Windows_Connect_Now.reg`.

New user profiles: covered when running in Sysprep mode.

### Enable DNS over HTTPS (DoH)

`-EnableDnsOverHttps`

Registry changes (`Enable_DNS_Over_HTTPS.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\Dnscache\Parameters` | `EnableAutoDoh` | 2 |

Reverting: unchecking this setting applies `Disable_DNS_Over_HTTPS.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable SMB bandwidth throttling

`-DisableSmbBandwidthThrottling`

Registry changes (`Disable_SMB_Throttling.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\LanmanWorkstation\Parameters` | `DisableBandwidthThrottling` | 1 |

Reverting: unchecking this setting applies `Enable_SMB_Throttling.reg`.

New user profiles: covered when running in Sysprep mode.

## Start Menu & Search

### Remove all pinned apps from the start menu for this user only

`-ClearStart`

*Version-gated setting.*

This setting is applied by dedicated logic in the script rather than by a registry file.

### Remove all pinned apps from the start menu for all existing and new users

`-ClearStartAllUsers`

*Version-gated setting.*

This setting is applied by dedicated logic in the script rather than by a registry file.

### Hide recommended section in the start menu

`-DisableStartRecommended`

*Version-gated setting.*

Registry changes (`Disable_Start_Recommended.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\SOFTWARE\Policies\Microsoft\Windows\Explorer` | `HideRecommendedSection` | 1 |

Reverting: unchecking this setting applies `Enable_Start_Recommended.reg`.

New user profiles: covered when running in Sysprep mode.

### Hide 'All Apps' section in the start menu

`-DisableStartAllApps`

*Version-gated setting.*

Registry changes (`Disable_Start_All_Apps.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Policies\Explorer` | `NoStartMenuMorePrograms` | 1 |

Reverting: unchecking this setting applies `Enable_Start_All_Apps.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Phone Link integration in the start menu

`-DisableStartPhoneLink`

*Version-gated setting.*

Registry changes (`Disable_Phone_Link_In_Start.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Start\Companions\Microsoft.YourPhone_8wekyb3d8bbwe` | `IsEnabled` | 0 |

Reverting: unchecking this setting applies `Enable_Phone_Link_In_Start.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Bing web search & Copilot integration in search

`-DisableBing`

Registry changes (`Disable_Bing_Cortana_In_Search.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Policies\Microsoft\Windows\Explorer` | `DisableSearchBoxSuggestions` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\Windows Search` | `AllowCortana` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\Windows Search` | `CortanaConsent` | 0 |

Reverting: unchecking this setting applies `Enable_Bing_Cortana_In_Search.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Windows search history

`-DisableSearchHistory`

Registry changes (`Disable_Search_History.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\SearchSettings` | `IsDeviceSearchHistoryEnabled` | 0 |

Reverting: unchecking this setting applies `Enable_Search_History.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable search highlights

`-DisableSearchHighlights`

Registry changes (`Disable_Search_Highlights.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\SearchSettings` | `IsDynamicSearchBoxEnabled` | 0 |

Reverting: unchecking this setting applies `Enable_Search_Highlights.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Microsoft Store app suggestions in search

`-DisableStoreSearchSuggestions`

*Version-gated setting.*

This setting is applied by dedicated logic in the script rather than by a registry file.

### Show All Apps in Category view (Default)

`-StartAllAppsCategory`

*Version-gated setting.*

Registry changes (`Start_AllApps_Category.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Start` | `AllAppsViewMode` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Policies\Explorer` | `NoStartMenuMorePrograms` | the value is deleted |

Reverting: this setting has no undo file and cannot be reverted by the script.

New user profiles: covered when running in Sysprep mode.

### Show All Apps in Grid view

`-StartAllAppsGrid`

*Version-gated setting.*

Registry changes (`Start_AllApps_Grid.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Start` | `AllAppsViewMode` | 1 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Policies\Explorer` | `NoStartMenuMorePrograms` | the value is deleted |

Reverting: unchecking this setting applies `Start_AllApps_Category.reg`.

New user profiles: covered when running in Sysprep mode.

### Show All Apps in List view

`-StartAllAppsList`

*Version-gated setting.*

Registry changes (`Start_AllApps_List.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Start` | `AllAppsViewMode` | 2 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Policies\Explorer` | `NoStartMenuMorePrograms` | the value is deleted |

Reverting: unchecking this setting applies `Start_AllApps_Category.reg`.

New user profiles: covered when running in Sysprep mode.

## AI

### Disable Microsoft Copilot

`-DisableCopilot`

*Version-gated setting.*

Registry changes (`Disable_Copilot.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `ShowCopilotButton` | 0 |
| `HKEY_CURRENT_USER\Software\Policies\Microsoft\Windows\WindowsCopilot` | `TurnOffWindowsCopilot` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\WindowsCopilot` | `TurnOffWindowsCopilot` | 1 |

Reverting: unchecking this setting applies `Enable_Copilot.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Windows Recall

`-DisableRecall`

*Version-gated setting.*

Registry changes (`Disable_AI_Recall.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Policies\Microsoft\Windows\WindowsAI` | `DisableAIDataAnalysis` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\WindowsAI` | `DisableAIDataAnalysis` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\WindowsAI` | `AllowRecallEnablement` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\WindowsAI` | `TurnOffSavingSnapshots` | 1 |

Reverting: unchecking this setting applies `Enable_AI_Recall.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Click To Do, AI text & image analysis

`-DisableClickToDo`

*Version-gated setting.*

Registry changes (`Disable_Click_to_Do.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Policies\Microsoft\Windows\WindowsAI` | `DisableClickToDo` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\WindowsAI` | `DisableClickToDo` | 1 |

Reverting: unchecking this setting applies `Enable_Click_to_Do.reg`.

New user profiles: covered when running in Sysprep mode.

### Prevent AI service from starting automatically

`-DisableAISvcAutoStart`

*Version-gated setting.*

Registry changes (`Disable_AI_Service_Auto_Start.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\WSAIFabricSvc` | `Start` | 3 |

Reverting: unchecking this setting applies `Enable_AI_Service_Auto_Start.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable AI features in Microsoft Edge

`-DisableEdgeAI`

*Version-gated setting.*

Registry changes (`Disable_Edge_AI_Features.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `CopilotCDPPageContext` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `CopilotPageContext` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `HubsSidebarEnabled` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `EdgeEntraCopilotPageContext` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `EdgeHistoryAISearchEnabled` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `ComposeInlineEnabled` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `GenAILocalFoundationalModelSettings` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Edge` | `NewTabPageBingChatEnabled` | 0 |

Reverting: unchecking this setting applies `Enable_Edge_AI_Features.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable AI features in Paint

`-DisablePaintAI`

*Version-gated setting.*

Registry changes (`Disable_Paint_AI_Features.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Paint` | `DisableCocreator` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Paint` | `DisableGenerativeFill` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Paint` | `DisableImageCreator` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Paint` | `DisableGenerativeErase` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Paint` | `DisableRemoveBackground` | 1 |

Reverting: unchecking this setting applies `Enable_Paint_AI_Features.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable AI features in Notepad

`-DisableNotepadAI`

*Version-gated setting.*

Registry changes (`Disable_Notepad_AI_Features.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\WindowsNotepad` | `DisableAIFeatures` | 1 |

Reverting: unchecking this setting applies `Enable_Notepad_AI_Features.reg`.

New user profiles: covered when running in Sysprep mode.

## Windows Update

### Prevent getting updates as soon as they're available

`-DisableUpdateASAP`

Registry changes (`Disable_Update_ASAP.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\WindowsUpdate\UX\Settings` | `IsContinuousInnovationOptedIn` | 0 |

Reverting: unchecking this setting applies `Enable_Update_ASAP.reg`.

New user profiles: covered when running in Sysprep mode.

### Prevent automatic restarts after updates while signed in

`-PreventUpdateAutoReboot`

Registry changes (`Prevent_Auto_Reboot.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\WindowsUpdate\AU` | `NoAutoRebootWithLoggedOnUsers` | 1 |

Reverting: unchecking this setting applies `Allow_Auto_Reboot.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable sharing downloaded updates with other PCs

`-DisableDeliveryOptimization`

Registry changes (`Disable_Delivery_Optimization.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_USERS\S-1-5-20\Software\Microsoft\Windows\CurrentVersion\DeliveryOptimization\Settings` | `DownloadMode` | 0 |

Reverting: unchecking this setting applies `Enable_Delivery_Optimization.reg`.

New user profiles: covered when running in Sysprep mode.

### Prevent Windows from auto-installing device companion apps

`-DisableDeviceAutoAppDownload`

Registry changes (`Disable_Device_Auto_App_Download.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\Device Metadata` | `PreventDeviceMetadataFromNetwork` | 1 |

Reverting: unchecking this setting applies `Enable_Device_Auto_App_Download.reg`.

New user profiles: covered when running in Sysprep mode.

## Taskbar

### Align taskbar to the left

`-TaskbarAlignLeft`

*Version-gated setting.*

Registry changes (`Align_Taskbar_Left.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `TaskbarAl` | 0 |

Reverting: unchecking this setting applies `Align_Taskbar_Center.reg`.

New user profiles: covered when running in Sysprep mode.

### Hide search icon from the taskbar

`-HideSearchTb`

*Version-gated setting.*

Registry changes (`Hide_Search_Taskbar.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Search` | `SearchboxTaskbarMode` | 0 |

Reverting: unchecking this setting applies `Show_Search_Box.reg`.

New user profiles: covered when running in Sysprep mode.

### Show search icon on the taskbar

`-ShowSearchIconTb`

*Version-gated setting.*

Registry changes (`Show_Search_Icon.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Search` | `SearchboxTaskbarMode` | 1 |

Reverting: unchecking this setting applies `Show_Search_Box.reg`.

New user profiles: covered when running in Sysprep mode.

### Show search icon with label on the taskbar

`-ShowSearchLabelTb`

*Version-gated setting.*

Registry changes (`Show_Search_Icon_And_Label.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Search` | `SearchboxTaskbarMode` | 3 |

Reverting: unchecking this setting applies `Show_Search_Box.reg`.

New user profiles: covered when running in Sysprep mode.

### Show search box on the taskbar

`-ShowSearchBoxTb`

*Version-gated setting.*

Registry changes (`Show_Search_Box.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Search` | `SearchboxTaskbarMode` | 2 |

Reverting: this setting has no undo file and cannot be reverted by the script.

New user profiles: covered when running in Sysprep mode.

### Hide 'Task view' button on the taskbar

`-HideTaskview`

*Version-gated setting.*

Registry changes (`Hide_Taskview_Taskbar.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `ShowTaskViewButton` | 0 |

Reverting: unchecking this setting applies `Show_Taskview_Taskbar.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable widgets on the taskbar & lock screen

`-DisableWidgets`

This setting is applied by dedicated logic in the script rather than by a registry file.

### Hide Chat (meet now) icon on the taskbar

`-HideChat`

*Version-gated setting.*

Registry changes (`Disable_Chat_Taskbar.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `TaskbarMn` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Policies\Explorer` | `HideSCAMeetNow` | 1 |

Reverting: unchecking this setting applies `Enable_Chat_Taskbar.reg`.

New user profiles: covered when running in Sysprep mode.

### Show 'End Task' option in taskbar context menu

`-EnableEndTask`

*Version-gated setting.*

Registry changes (`Enable_End_Task.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced\TaskbarDeveloperSettings` | `TaskbarEndTask` | 1 |

Reverting: unchecking this setting applies `Disable_End_Task.reg`.

New user profiles: covered when running in Sysprep mode.

### Enable 'Last Active Click' behavior for taskbar apps

`-EnableLastActiveClick`

*Version-gated setting.*

Registry changes (`Enable_Last_Active_Click.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `LastActiveClick` | 1 |

Reverting: unchecking this setting applies `Disable_Last_Active_Click.reg`.

New user profiles: covered when running in Sysprep mode.

### Always combine taskbar buttons and hide labels for the main display

`-CombineTaskbarAlways`

*Version-gated setting.*

Registry changes (`Combine_Taskbar_Always.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `TaskbarGlomLevel` | 0 |

Reverting: this setting has no undo file and cannot be reverted by the script.

New user profiles: covered when running in Sysprep mode.

### Always combine taskbar buttons and hide labels for secondary displays

`-CombineMMTaskbarAlways`

*Version-gated setting.*

Registry changes (`Combine_MMTaskbar_Always.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `MMTaskbarGlomLevel` | 0 |

Reverting: this setting has no undo file and cannot be reverted by the script.

New user profiles: covered when running in Sysprep mode.

### Combine taskbar buttons and hide labels when taskbar is full for the main display

`-CombineTaskbarWhenFull`

*Version-gated setting.*

Registry changes (`Combine_Taskbar_When_Full.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `TaskbarGlomLevel` | 1 |

Reverting: unchecking this setting applies `Combine_Taskbar_Always.reg`.

New user profiles: covered when running in Sysprep mode.

### Combine taskbar buttons and hide labels when taskbar is full for secondary displays

`-CombineMMTaskbarWhenFull`

*Version-gated setting.*

Registry changes (`Combine_MMTaskbar_When_Full.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `MMTaskbarGlomLevel` | 1 |

Reverting: unchecking this setting applies `Combine_MMTaskbar_Always.reg`.

New user profiles: covered when running in Sysprep mode.

### Never combine taskbar buttons and show labels for the main display

`-CombineTaskbarNever`

*Version-gated setting.*

Registry changes (`Combine_Taskbar_Never.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `TaskbarGlomLevel` | 2 |

Reverting: unchecking this setting applies `Combine_Taskbar_Always.reg`.

New user profiles: covered when running in Sysprep mode.

### Never combine taskbar buttons and show labels for secondary displays

`-CombineMMTaskbarNever`

*Version-gated setting.*

Registry changes (`Combine_MMTaskbar_Never.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `MMTaskbarGlomLevel` | 2 |

Reverting: unchecking this setting applies `Combine_MMTaskbar_Always.reg`.

New user profiles: covered when running in Sysprep mode.

### Show app icons on all taskbars

`-MMTaskbarModeAll`

*Version-gated setting.*

Registry changes (`MMTaskbarMode_All.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `MMTaskbarMode` | 0 |

Reverting: this setting has no undo file and cannot be reverted by the script.

New user profiles: covered when running in Sysprep mode.

### Show app icons on main taskbar and on taskbar where the windows is open

`-MMTaskbarModeMainActive`

*Version-gated setting.*

Registry changes (`MMTaskbarMode_Main_Active.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `MMTaskbarMode` | 1 |

Reverting: unchecking this setting applies `MMTaskbarMode_All.reg`.

New user profiles: covered when running in Sysprep mode.

### Show app icons only on taskbar where the window is open

`-MMTaskbarModeActive`

*Version-gated setting.*

Registry changes (`MMTaskbarMode_Active.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `MMTaskbarMode` | 2 |

Reverting: unchecking this setting applies `MMTaskbarMode_All.reg`.

New user profiles: covered when running in Sysprep mode.

## Appearance

### Enable dark theme for system and apps

`-EnableDarkMode`

Registry changes (`Enable_Dark_Mode.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Themes\Personalize` | `AppsUseLightTheme` | 0 |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Themes\Personalize` | `SystemUsesLightTheme` | 0 |

Reverting: unchecking this setting applies `Enable_Light_Mode.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable transparency effects

`-DisableTransparency`

Registry changes (`Disable_Transparency.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Themes\Personalize` | `EnableTransparency` | 0 |

Reverting: unchecking this setting applies `Enable_Transparency.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable animations and visual effects

`-DisableAnimations`

Registry changes (`Disable_Animations.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Control Panel\Desktop` | `UserPreferencesMask` | hex:90,12,07,80,10,00,00,00 |

Reverting: unchecking this setting applies `Enable_Animations.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Dynamic Lighting (HID lighting)

`-DisableDynamicLighting`

Registry changes (`Disable_Dynamic_Lighting.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Lighting` | `AmbientLightingEnabled` | 0 |

Reverting: unchecking this setting applies `Enable_Dynamic_Lighting.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable visual effects for best performance

`-DisableVisualEffects`

Registry changes (`Disable_Visual_Effects.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Control Panel\Desktop` | `FontSmoothing` | "2" |
| `HKEY_CURRENT_USER\Control Panel\Desktop` | `DragFullWindows` | "1" |
| `HKEY_CURRENT_USER\Control Panel\Desktop\WindowMetrics` | `MinAnimate` | "0" |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `ListviewAlphaSelect` | 1 |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `IconsOnly` | 0 |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `TaskbarAnimations` | 0 |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `ListviewShadow` | 1 |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\VisualEffects` | `VisualFXSetting` | 3 |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\DWM` | `EnableAeroPeek` | 0 |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\DWM` | `AlwaysHibernateThumbnails` | 0 |

Reverting: unchecking this setting applies `Enable_Visual_Effects.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Aero Shake

`-DisableAeroShake`

Registry changes (`Disable_Aero_Shake.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `DisallowShaking` | 1 |

Reverting: unchecking this setting applies `Enable_Aero_Shake.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable desktop peek (Aero Peek)

`-DisableDesktopPeek`

Registry changes (`Disable_Desktop_Peek.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `DisablePreviewDesktop` | 1 |

Reverting: unchecking this setting applies `Enable_Desktop_Peek.reg`.

New user profiles: covered when running in Sysprep mode.

### Use compact mode in File Explorer

`-UseCompactModeExplorer`

Registry changes (`Use_Compact_Mode.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `UseCompactMode` | 1 |

Reverting: unchecking this setting applies `Use_Ribbon_Mode.reg`.

New user profiles: covered when running in Sysprep mode.

## File Explorer

### Change the default location that File Explorer opens to 'Home'

`-ExplorerToHome`

Registry changes (`Launch_File_Explorer_To_Home.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `LaunchTo` | 2 |

Reverting: this setting has no undo file and cannot be reverted by the script.

New user profiles: covered when running in Sysprep mode.

### Change the default location that File Explorer opens to 'This PC'

`-ExplorerToThisPC`

Registry changes (`Launch_File_Explorer_To_This_PC.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `LaunchTo` | 1 |

Reverting: unchecking this setting applies `Launch_File_Explorer_To_Home.reg`.

New user profiles: covered when running in Sysprep mode.

### Change the default location that File Explorer opens to 'Downloads'

`-ExplorerToDownloads`

Registry changes (`Launch_File_Explorer_To_Downloads.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `LaunchTo` | 3 |

Reverting: unchecking this setting applies `Launch_File_Explorer_To_Home.reg`.

New user profiles: covered when running in Sysprep mode.

### Change the default location that File Explorer opens to 'OneDrive'

`-ExplorerToOneDrive`

Registry changes (`Launch_File_Explorer_To_OneDrive.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `LaunchTo` | 4 |

Reverting: unchecking this setting applies `Launch_File_Explorer_To_Home.reg`.

New user profiles: covered when running in Sysprep mode.

### Show file extensions for known file types

`-ShowKnownFileExt`

Registry changes (`Show_Extensions_For_Known_File_Types.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `HideFileExt` | 0 |

Reverting: unchecking this setting applies `Hide_Extensions_For_Known_File_Types.reg`.

New user profiles: covered when running in Sysprep mode.

### Show hidden files, folders and drives

`-ShowHiddenFolders`

Registry changes (`Show_Hidden_Folders.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `Hidden` | 1 |

Reverting: unchecking this setting applies `Hide_Hidden_Folders.reg`.

New user profiles: covered when running in Sysprep mode.

### Hide duplicate removable drive entries

`-HideDupliDrive`

Registry changes (`Hide_duplicate_removable_drives_from_navigation_pane_of_File_Explorer.reg`):

| Key | Value | Set to |
| --- | --- | --- |

Reverting: unchecking this setting applies `Show_duplicate_removable_drives_in_navigation_pane_of_File_Explorer.reg`.

New user profiles: covered when running in Sysprep mode.

### Hide 'Home' from navigation pane

`-HideHome`

*Version-gated setting.*

Registry changes (`Hide_Home_from_Explorer.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Classes\CLSID\{f874310e-b6b7-47dc-bc84-b9e6b38f5903}` | `(Default)` | "CLSID_MSGraphHomeFolder" |
| `HKEY_CURRENT_USER\Software\Classes\CLSID\{f874310e-b6b7-47dc-bc84-b9e6b38f5903}` | `System.IsPinnedToNameSpaceTree` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowHome` | `CheckedValue` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowHome` | `DefaultValue` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowHome` | `HKeyRoot` | 2147483649 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowHome` | `Id` | 13 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowHome` | `RegPath` | "Software\\Classes\\CLSID\\{f874310e-b6b7-47dc-bc84-b9e6b38f5903}" |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowHome` | `Text` | "Show Home" |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowHome` | `Type` | "checkbox" |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowHome` | `UncheckedValue` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowHome` | `ValueName` | "System.IsPinnedToNameSpaceTree" |

Reverting: unchecking this setting applies `Show_Home_in_Explorer.reg`.

New user profiles: covered when running in Sysprep mode.

### Hide 'Gallery' from navigation pane

`-HideGallery`

*Version-gated setting.*

Registry changes (`Hide_Gallery_from_Explorer.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Classes\CLSID\{e88865ea-0e1c-4e20-9aa6-edcd0212c87c}` | `System.IsPinnedToNameSpaceTree` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowGallery` | `CheckedValue` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowGallery` | `DefaultValue` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowGallery` | `HKeyRoot` | 2147483649 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowGallery` | `Id` | 13 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowGallery` | `RegPath` | "Software\\Classes\\CLSID\\{e88865ea-0e1c-4e20-9aa6-edcd0212c87c}" |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowGallery` | `Text` | "Show Gallery" |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowGallery` | `Type` | "checkbox" |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowGallery` | `UncheckedValue` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowGallery` | `ValueName` | "System.IsPinnedToNameSpaceTree" |

Reverting: unchecking this setting applies `Show_Gallery_in_Explorer.reg`.

New user profiles: covered when running in Sysprep mode.

### Hide 'OneDrive' from navigation pane

`-HideOnedrive`

Registry changes (`Hide_Onedrive_Folder.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Classes\CLSID\{018D5C66-4533-4307-9B53-224DE2ED1FE6}` | `System.IsPinnedToNameSpaceTree` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowOneDrive` | `CheckedValue` | 1 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowOneDrive` | `DefaultValue` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowOneDrive` | `HKeyRoot` | 2147483649 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowOneDrive` | `Id` | 13 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowOneDrive` | `RegPath` | "Software\\Classes\\CLSID\\{018D5C66-4533-4307-9B53-224DE2ED1FE6}" |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowOneDrive` | `Text` | "Show OneDrive" |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowOneDrive` | `Type` | "checkbox" |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowOneDrive` | `UncheckedValue` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced\NavPane\ShowOneDrive` | `ValueName` | "System.IsPinnedToNameSpaceTree" |

Reverting: unchecking this setting applies `Show_Onedrive_Folder.reg`.

New user profiles: covered when running in Sysprep mode.

### Hide '3D objects' folder under 'This PC'

`-Hide3dObjects`

*Version-gated setting.*

Registry changes (`Hide_3D_Objects_Folder.reg`):

| Key | Value | Set to |
| --- | --- | --- |

Reverting: unchecking this setting applies `Show_3D_Objects_Folder.reg`.

New user profiles: covered when running in Sysprep mode.

### Hide 'Music' folder under 'This PC'

`-HideMusic`

*Version-gated setting.*

Registry changes (`Hide_Music_Folder.reg`):

| Key | Value | Set to |
| --- | --- | --- |

Reverting: unchecking this setting applies `Show_Music_Folder.reg`.

New user profiles: covered when running in Sysprep mode.

### Add common folders back to 'This PC' page

`-AddFoldersToThisPC`

*Version-gated setting.*

Registry changes (`Add_All_Folders_Under_This_PC.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\MyComputer\NameSpace\{B4BFCC3A-DB2C-424C-B029-7FE99A87C641}` | `HideIfEnabled` | the value is deleted |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\MyComputer\NameSpace\{B4BFCC3A-DB2C-424C-B029-7FE99A87C641}` | `HiddenByDefault` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\MyComputer\NameSpace\{d3162b92-9365-467a-956b-92703aca08af}` | `HideIfEnabled` | the value is deleted |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\MyComputer\NameSpace\{d3162b92-9365-467a-956b-92703aca08af}` | `HiddenByDefault` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\MyComputer\NameSpace\{088e3905-0323-4b02-9826-5d99428e115f}` | `HideIfEnabled` | the value is deleted |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\MyComputer\NameSpace\{088e3905-0323-4b02-9826-5d99428e115f}` | `HiddenByDefault` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\MyComputer\NameSpace\{3dfdf296-dbec-4fb4-81d1-6a3438bcf4de}` | `HideIfEnabled` | the value is deleted |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\MyComputer\NameSpace\{1CF1260C-4DD0-4ebb-811F-33C572699FDE}` | `HiddenByDefault` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\MyComputer\NameSpace\{3dfdf296-dbec-4fb4-81d1-6a3438bcf4de}` | `HiddenByDefault` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\MyComputer\NameSpace\{24ad3ad4-a569-4530-98e1-ab02f9417aa8}` | `HideIfEnabled` | the value is deleted |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\MyComputer\NameSpace\{24ad3ad4-a569-4530-98e1-ab02f9417aa8}` | `HiddenByDefault` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\MyComputer\NameSpace\{f86fa3ab-70d2-4fc7-9c99-fcbf05467f3a}` | `HideIfEnabled` | the value is deleted |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\MyComputer\NameSpace\{f86fa3ab-70d2-4fc7-9c99-fcbf05467f3a}` | `HiddenByDefault` | 0 |

Reverting: unchecking this setting applies `Remove_All_Folders_Under_This_PC.reg`.

New user profiles: covered when running in Sysprep mode.

### Hide 'Include in library' option in the context menu

`-HideIncludeInLibrary`

*Version-gated setting.*

Registry changes (`Disable_Include_in_library_from_context_menu.reg`):

| Key | Value | Set to |
| --- | --- | --- |

Reverting: unchecking this setting applies `Enable_Include_in_library_in_context_menu.reg`.

New user profiles: covered when running in Sysprep mode.

### Hide 'Give access to' option in the context menu

`-HideGiveAccessTo`

*Version-gated setting.*

Registry changes (`Disable_Give_access_to_context_menu.reg`):

| Key | Value | Set to |
| --- | --- | --- |

Reverting: unchecking this setting applies `Enable_Give_access_to_context_menu.reg`.

New user profiles: covered when running in Sysprep mode.

### Hide 'Share' option in the context menu

`-HideShare`

*Version-gated setting.*

Registry changes (`Disable_Share_from_context_menu.reg`):

| Key | Value | Set to |
| --- | --- | --- |

Reverting: unchecking this setting applies `Enable_Share_in_context_menu.reg`.

New user profiles: covered when running in Sysprep mode.

### Show drive letters before drive label

`-ShowDriveLettersFirst`

Registry changes (`Show_Drive_Letters_First.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer` | `ShowDriveLettersFirst` | 4 |

Reverting: unchecking this setting applies `Show_Drive_Letters_Last.reg`.

New user profiles: covered when running in Sysprep mode.

### Show drive letters after drive label

`-ShowDriveLettersLast`

Registry changes (`Show_Drive_Letters_Last.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer` | `ShowDriveLettersFirst` | 0 |

Reverting: this setting has no undo file and cannot be reverted by the script.

New user profiles: covered when running in Sysprep mode.

### Show network drive letters before drive label

`-ShowNetworkDriveLettersFirst`

Registry changes (`Show_Network_Drive_Letters_First.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer` | `ShowDriveLettersFirst` | 1 |

Reverting: unchecking this setting applies `Show_Drive_Letters_Last.reg`.

New user profiles: covered when running in Sysprep mode.

### Hide all drive letters

`-HideDriveLetters`

Registry changes (`Hide_Drive_Letters.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer` | `ShowDriveLettersFirst` | 2 |

Reverting: unchecking this setting applies `Show_Drive_Letters_Last.reg`.

New user profiles: covered when running in Sysprep mode.

### Hide recent files in Quick Access

`-HideRecentFiles`

Registry changes (`Hide_Recent_Files.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `ShowRecent` | 0 |

Reverting: unchecking this setting applies `Show_Recent_Files.reg`.

New user profiles: covered when running in Sysprep mode.

### Optimise the thumbnail cache size

`-SetThumbnailCacheSize`

Registry changes (`Set_Thumbnail_Cache_Size.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer` | `ThumbnailCacheMaxSizeMb` | 200 |

Reverting: unchecking this setting applies `Default_Thumbnail_Cache_Size.reg`.

New user profiles: covered when running in Sysprep mode.

## Gaming

### Disable Xbox game/screen recording

`-DisableDVR`

Registry changes (`Disable_DVR.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\System\GameConfigStore` | `GameDVR_Enabled` | 0 |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\GameDVR` | `AppCaptureEnabled` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\GameDVR` | `AllowGameDVR` | 0 |

Reverting: unchecking this setting applies `Enable_DVR.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Game Bar integration

`-DisableGameBarIntegration`

Registry changes (`Disable_Game_Bar_Integration.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\GameBar` | `UseNexusForGameBarEnabled` | 0 |
| `HKEY_CLASSES_ROOT\ms-gamebar` | `(Default)` | "URL:ms-gamebar" |
| `HKEY_CLASSES_ROOT\ms-gamebar` | `URL Protocol` | "" |
| `HKEY_CLASSES_ROOT\ms-gamebar` | `NoOpenWith` | "" |
| `HKEY_CLASSES_ROOT\ms-gamebar\shell\open\command` | `(Default)` | "%SystemRoot%/System32/systray.exe" |
| `HKEY_CLASSES_ROOT\ms-gamebarservices` | `(Default)` | "URL:ms-gamebarservices" |
| `HKEY_CLASSES_ROOT\ms-gamebarservices` | `URL Protocol` | "" |
| `HKEY_CLASSES_ROOT\ms-gamebarservices` | `NoOpenWith` | "" |
| `HKEY_CLASSES_ROOT\ms-gamebarservices\shell\open\command` | `(Default)` | "%SystemRoot%/System32/systray.exe" |

Reverting: unchecking this setting applies `Enable_Game_Bar_Integration.reg`.

New user profiles: covered when running in Sysprep mode.

## Multi-tasking

### Disable window snapping

`-DisableWindowSnapping`

Registry changes (`Disable_Window_Snapping.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Control Panel\Desktop` | `WindowArrangementActive` | "0" |

Reverting: unchecking this setting applies `Enable_Window_Snapping.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable showing app suggestions when snapping windows

`-DisableSnapAssist`

*Version-gated setting.*

Registry changes (`Disable_Snap_Assist.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `SnapAssist` | 0 |

Reverting: unchecking this setting applies `Enable_Snap_Assist.reg`.

New user profiles: covered when running in Sysprep mode.

### Hide snap layout flyout at top of screen and on maximize button

`-DisableSnapLayouts`

*Version-gated setting.*

Registry changes (`Disable_Snap_Layouts.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `EnableSnapBar` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `EnableSnapAssistFlyout` | 0 |

Reverting: unchecking this setting applies `Enable_Snap_Layouts.reg`.

New user profiles: covered when running in Sysprep mode.

### Hide tabs from apps when snapping or pressing Alt+Tab

`-HideTabsInAltTab`

*Version-gated setting.*

Registry changes (`Hide_Tabs_In_Alt_Tab.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `MultiTaskingAltTabFilter` | 3 |

Reverting: this setting has no undo file and cannot be reverted by the script.

New user profiles: covered when running in Sysprep mode.

### Show 3 tabs from apps when snapping or pressing Alt+Tab

`-Show3TabsInAltTab`

*Version-gated setting.*

Registry changes (`Show_3_Tabs_In_Alt_Tab.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `MultiTaskingAltTabFilter` | 2 |

Reverting: this setting has no undo file and cannot be reverted by the script.

New user profiles: covered when running in Sysprep mode.

### Show 5 tabs from apps when snapping or pressing Alt+Tab

`-Show5TabsInAltTab`

*Version-gated setting.*

Registry changes (`Show_5_Tabs_In_Alt_Tab.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `MultiTaskingAltTabFilter` | 1 |

Reverting: this setting has no undo file and cannot be reverted by the script.

New user profiles: covered when running in Sysprep mode.

### Show 20 tabs from apps when snapping or pressing Alt+Tab

`-Show20TabsInAltTab`

*Version-gated setting.*

Registry changes (`Show_20_Tabs_In_Alt_Tab.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced` | `MultiTaskingAltTabFilter` | 0 |

Reverting: this setting has no undo file and cannot be reverted by the script.

New user profiles: covered when running in Sysprep mode.

## Optional Windows Features

### Enable Windows Sandbox

`-EnableWindowsSandbox`

*Version-gated setting.*

This setting is applied by dedicated logic in the script rather than by a registry file.

### Enable Windows Subsystem for Linux

`-EnableWindowsSubsystemForLinux`

*Version-gated setting.*

This setting is applied by dedicated logic in the script rather than by a registry file.

## Other

### Hide Settings 'Home' page

`-DisableSettingsHome`

*Version-gated setting.*

Registry changes (`Disable_Settings_Home.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Explorer` | `SettingsPageVisibility` | "hide:home" |

Reverting: unchecking this setting applies `Enable_Settings_Home.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable bloat in Brave browser (AI, Crypto, etc.)

`-DisableBraveBloat`

Registry changes (`Disable_Brave_Bloat.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\Software\Policies\BraveSoftware\Brave` | `BraveVPNDisabled` | 1 |
| `HKEY_LOCAL_MACHINE\Software\Policies\BraveSoftware\Brave` | `BraveWalletDisabled` | 1 |
| `HKEY_LOCAL_MACHINE\Software\Policies\BraveSoftware\Brave` | `BraveAIChatEnabled` | 0 |
| `HKEY_LOCAL_MACHINE\Software\Policies\BraveSoftware\Brave` | `BraveRewardsDisabled` | 1 |
| `HKEY_LOCAL_MACHINE\Software\Policies\BraveSoftware\Brave` | `BraveTalkDisabled` | 1 |
| `HKEY_LOCAL_MACHINE\Software\Policies\BraveSoftware\Brave` | `BraveNewsDisabled` | 1 |

Reverting: unchecking this setting applies `Enable_Brave_Bloat.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Cross-Device Resume

`-DisableCrossDeviceResume`

Registry changes (`Disable_Cross_Device_Resume.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\CrossDeviceResume\Configuration` | `IsResumeAllowed` | 0 |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\PolicyManager\default\Connectivity\DisableCrossDeviceResume` | `Value` | 1 |

Reverting: unchecking this setting applies `Enable_Cross_Device_Resume.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable tips in the Settings app

`-DisableSettingsTips`

Registry changes (`Disable_Settings_Tips.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\PolicyManager\default\Settings\AllowOnlineTips` | `value` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Explorer` | `AllowOnlineTips` | 0 |

Reverting: unchecking this setting applies `Enable_Settings_Tips.reg`.

New user profiles: covered when running in Sysprep mode.

### Enable long file paths (NTFS)

`-EnableLongPaths`

Registry changes (`Enable_Long_Paths.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\FileSystem` | `LongPathsEnabled` | 1 |

Reverting: unchecking this setting applies `Disable_Long_Paths.reg`.

New user profiles: covered when running in Sysprep mode.

### Use classic search in File Explorer

`-UseClassicSearch`

Registry changes (`Use_Classic_Search.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Classes\CLSID\{1d64637d-31e9-4b06-9124-e83fb178ac6e}\TreatAs` | `(Default)` | "{64bc32b5-4eec-4de7-972d-bd8bd0324537}" |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Classes\WOW6432Node\CLSID\{1d64637d-31e9-4b06-9124-e83fb178ac6e}\TreatAs` | `(Default)` | "{64bc32b5-4eec-4de7-972d-bd8bd0324537}" |
| `HKEY_LOCAL_MACHINE\SOFTWARE\WOW6432Node\Classes\CLSID\{1d64637d-31e9-4b06-9124-e83fb178ac6e}\TreatAs` | `(Default)` | "{64bc32b5-4eec-4de7-972d-bd8bd0324537}" |

Reverting: unchecking this setting applies `Use_Modern_Search.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Nearby Sharing

`-DisableNearbySharing`

Registry changes (`Disable_Nearby_Sharing.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\CDP\SettingsPage` | `BluetoothLastDisabledNearShare` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\CDP` | `NearShareChannelUserAuthzPolicy` | 0 |
| `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\CDP` | `CdpSessionUserAuthzPolicy` | 1 |

Reverting: unchecking this setting applies `Enable_Nearby_Sharing.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable News and Interests

`-DisableNewsAndInterests`

Registry changes (`Disable_News_And_Interests.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\Windows Feeds` | `EnableFeeds` | 0 |
| `HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Dsh` | `AllowNewsAndInterests` | 0 |

Reverting: unchecking this setting applies `Enable_News_And_Interests.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable Tablet Mode

`-DisableTabletMode`

Registry changes (`Disable_Tablet_Mode.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\ImmersiveShell` | `SignInMode` | 1 |

Reverting: unchecking this setting applies `Enable_Tablet_Mode.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable low disk space warnings

`-DisableLowDiskWarning`

Registry changes (`Disable_Low_Disk_Warning.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Explorer` | `NoLowDiskSpaceChecks` | 1 |

Reverting: unchecking this setting applies `Enable_Low_Disk_Warning.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable startup delay for apps

`-DisableStartupDelay`

Registry changes (`Disable_Startup_Delay.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Serialize` | `StartupDelayInMSec` | 0 |

Reverting: unchecking this setting applies `Enable_Startup_Delay.reg`.

New user profiles: covered when running in Sysprep mode.

### Disable WPBT (firmware-injected binaries)

`-DisableWpbt`

Registry changes (`Disable_WPBT.reg`):

| Key | Value | Set to |
| --- | --- | --- |
| `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\Session Manager` | `DisableWpbtExecution` | 1 |

Reverting: unchecking this setting applies `Enable_WPBT.reg`.

New user profiles: covered when running in Sysprep mode.

## Other settings

These are selected through dedicated command-line switches rather than the settings list.

### Remove the apps specified with the 'Apps' parameter

`-RemoveApps`

This setting is applied by dedicated logic in the script rather than by a registry file.

### The selection of apps to remove, specified as a comma separated list. Use 'Default' (or omit) to use the default apps list

`-Apps`

This setting is applied by dedicated logic in the script rather than by a registry file.

### Remove the Xbox App and Xbox Gamebar

`-RemoveGamingApps`

This setting is applied by dedicated logic in the script rather than by a registry file.

### Remove HP OEM applications

`-RemoveHPApps`

This setting is applied by dedicated logic in the script rather than by a registry file.

### Create a system restore point

`-CreateRestorePoint`

This setting is applied by dedicated logic in the script rather than by a registry file.

### Replace the start menu layout for this user only with the provided template file

`-ReplaceStart`

*Version-gated setting.*

This setting is applied by dedicated logic in the script rather than by a registry file.

### Replace the start menu layout for all existing and new users with the provided template file

`-ReplaceStartAllUsers`

*Version-gated setting.*

This setting is applied by dedicated logic in the script rather than by a registry file.

### Forcefully uninstall Microsoft Edge. NOT RECOMMENDED!

`-ForceRemoveEdge`

This setting is applied by dedicated logic in the script rather than by a registry file.

